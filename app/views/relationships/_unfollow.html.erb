<%= form_for(current_user.active_relationships.find_by(followed_id: friend.id),
             html: { method: :delete, remote: true }) do |f| %>
  <%= f.submit "解除", class: "square_btn delete" %>
<% end %>