<div class="box text-center">
  <div class="box-contents">
    <h5>マッチリスト<i class="fa fa-heart" style="color: rgba(255,139,137,0.98);"></i></h5>
  </div>
</div>
<%- if @match_users.blank? %>
  <div class="box text-center">
    <div class="box-contents">
      <p>まだマッチしている人が居ません。</p>
      <p>見つかるようにシェアしよう！</p>
      <%= render "shared/twitter_share_button" %>
    </div>
  </div>
<%- end %>
<%- if @number_of_followers - @match_users.count != 0 %>
  <div class="box text-center">
    <div class="box-contents">
      <p>あなたの事を気になっているユーザーは<br>現在 <%= @number_of_followers - @match_users.count %> 名います。</p>
    </div>
  </div>
<%- end %>

<%- @match_users.each do |user| %>
  <div class="box">
    <div class="box-contents" style="display: flex">
      <div class="friend-box">
        <%= image_tag user.image.gsub("_normal", ""), size: 50, class: "img-responsive-overwrite", align: "middle" %>
        <span><%= user.name %></span>
      </div>
    <div>
      <button class="square_btn">
        <a href="https://twitter.com/<%= user.nickname %>" style="color: #67c5ff;">DMを送る</a>
      </button>
    </div>
    </div>
  </div>
<% end %>